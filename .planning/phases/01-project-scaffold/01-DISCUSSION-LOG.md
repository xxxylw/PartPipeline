# Phase 1 Discussion Log

User confirmed:
- Project name and path.
- Fork URLs for SAMPart3D and HoloPart.
- Default SAMPart3D mask scale `1.0` for v1.
- Single-file and batch commands are both required.
- Outputs should be complete enough for presentation.
- A shared environment should be investigated, with env dispatch as fallback.
